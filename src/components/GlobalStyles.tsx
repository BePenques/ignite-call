"use client";
import { globalStyles } from "@/styles/global";

 // Agora esse arquivo é um Client Component



export function GlobalStyles() {
  globalStyles() // Aplica os estilos globais
  return null // Esse componente não renderiza nada, só aplica os estilos
}